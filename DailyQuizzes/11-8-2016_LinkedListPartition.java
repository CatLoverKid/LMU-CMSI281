public class 11-8-2016_LinkedListPartition{
  public LinkedList partitionList(Node partitionValue){
    LinkedList tempList = new LinkedList()
    partitionNode.nest = first;
    while(partitionNode.next != null){
      if(partitionNode.next.value >= partitionNode.value){
        tempList.add(partitionNode.next.value;
	partitionNode.next = partitionNode.next
	remove(partitionNode);
      }else{
        partitionNode.next = partitionNode.next.next;
      }
    }
  //add the partitionValue

  //stitch together the two lists
  }
}
